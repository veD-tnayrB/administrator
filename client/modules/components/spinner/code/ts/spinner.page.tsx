import React from 'react';
import { Spinner } from './spinner.index';

export /*bundle*/ const SpinnerPage = () => {
	return (
		<div className="essential-spinner-page">
			<Spinner />
		</div>
	);
};
