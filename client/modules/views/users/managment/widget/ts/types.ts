export interface ITexts {
	labels: {
		names: string;
		lastNames: string;
		email: string;
		active: string;
	};
	title: {
		creating: string;
		editing: string;
	};
	actions: {
		reset: string;
		save: string;
	};
}
